# 갤러리 이미지 최적화 및 보호 기능 가이드

## 🖼️ 이미지 압축 도구 사용법

### Windows 사용자
1. `compress_images.bat` 파일을 더블클릭하여 실행
2. 메시지가 나타나면 'y'를 입력하여 진행

### Mac/Linux 사용자
```bash
# Python 설치 확인
python3 --version

# Pillow 라이브러리 설치
pip3 install Pillow

# 압축 스크립트 실행
python3 compress_images.py
```

### 압축 결과
- **원본 백업**: `gallery_backup/` 폴더에 저장
- **압축된 이미지**: `gallery_compressed/` 폴더에 저장
- **예상 압축률**: 80-90% (6-12MB → 200-500KB)
- **품질 설정**: 85% (높은 품질 유지)
- **최대 크기**: 1920x1080 픽셀

## 🔒 이미지 보호 기능

### 구현된 보호 기능
1. **우클릭 방지**: 이미지 우클릭 시 컨텍스트 메뉴 차단
2. **드래그 방지**: 이미지를 드래그할 수 없음
3. **선택 방지**: 이미지 선택 불가
4. **확대 방지**: 갤러리 모달 비활성화
5. **모바일 핀치줌 방지**: 터치 이벤트로 확대 불가
6. **키보드 단축키 차단**: Ctrl+S, Ctrl+P, F12 차단

### 추가 보안 옵션 (선택적)
개발자 도구 감지 기능을 활성화하려면:

```javascript
// script.js 파일에서 주석 해제
detectDevTools();
```

## 📱 성능 최적화

### Lazy Loading 적용
- 화면에 보이는 이미지만 우선 로드
- 스크롤 시 추가 이미지 로드
- 초기 로딩 속도 개선

### 모바일 최적화
- viewport 설정으로 확대/축소 방지
- 터치 이벤트 최적화
- 모바일 친화적 UI

## ⚠️ 주의사항

1. **이미지 압축 전 반드시 백업**
   - 스크립트는 자동으로 백업을 생성하지만 추가 백업 권장

2. **브라우저 호환성**
   - 모든 최신 브라우저에서 작동
   - IE11 이하는 일부 기능 제한

3. **완벽한 보호는 불가능**
   - 웹에 공개된 이미지는 기술적으로 완전한 보호 불가능
   - 일반 사용자의 다운로드를 어렵게 만드는 수준

## 🔧 문제 해결

### 압축 스크립트가 작동하지 않을 때
1. Python 설치 확인
2. Pillow 라이브러리 재설치: `pip install --upgrade Pillow`
3. 이미지 파일 권한 확인

### 이미지가 표시되지 않을 때
1. 브라우저 캐시 삭제
2. 개발자 도구 콘솔에서 오류 확인
3. 이미지 경로 확인

## 📞 지원

추가 도움이 필요하시면 웹사이트 관리자에게 문의하세요.